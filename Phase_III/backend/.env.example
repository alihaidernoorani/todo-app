# Database Configuration
# Get your connection string from https://console.neon.tech
NEON_DATABASE_URL=postgresql://user:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=true

# Better Auth Configuration
# Base URL where Better Auth is hosted (e.g., https://app.example.com or http://localhost:3000 for development)
BETTER_AUTH_URL=http://localhost:3000

# Better Auth JWT Secret (required for HS256 signature verification in development)
# IMPORTANT: Use RS256 with JWKS in production for better security
# - RS256 (JWKS): Public/private key cryptography - recommended for production
# - HS256 (Shared Secret): Symmetric key - easier for local development
# Generate with: openssl rand -hex 32
BETTER_AUTH_SECRET=your-secret-key-min-32-chars-long-replace-this

# The JWKS endpoint URL will be constructed as ${BETTER_AUTH_URL}/api/auth/.well-known/jwks.json
# No need to set this manually as it's constructed from BETTER_AUTH_URL
# Note: Better Auth endpoints in Next.js are prefixed with /api/auth/

# CORS Configuration
# Comma-separated list of allowed frontend origins for CORS
# For local development: http://localhost:3000
# For production: https://your-app.vercel.app,https://your-app-git-*.vercel.app
# Note: Vercel preview deployments (https://*.vercel.app) are automatically allowed via regex pattern
# DO NOT add spaces after commas
ALLOWED_ORIGINS=http://localhost:3000,https://todo-app-mu-two-48.vercel.app


# OpenAI Agents SDK Configuration (OpenRouter)
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=your-openrouter-api-key-here
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
AGENT_MODEL=openai/gpt-4o
AGENT_MAX_TOKENS=2000

# Internal backend URL for agent self-calls (MCP tools → REST endpoints)
# Must match the port uvicorn is listening on:
#   - HF Spaces (Dockerfile CMD): http://localhost:7860  (default)
#   - Local dev: http://localhost:8000
BACKEND_BASE_URL=http://localhost:7860
